import { ClickHighlightDirective } from './click-highlight.directive';

describe('ClickHighlightDirective', () => {
  it('should create an instance', () => {
    const directive = new ClickHighlightDirective();
    expect(directive).toBeTruthy();
  });
});
